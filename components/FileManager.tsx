'use client'
import FileUpload from './FileUpload';

const Home: React.FC = () => {
  return (
    <div className="min-h-screen bg-gray-100 p-12">
    <FileUpload />
  </div>
  );
};

export default Home;
